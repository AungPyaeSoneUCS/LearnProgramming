---
title: "MySQL Examples — Examples"
description: "MySQL Examples — လက်တွေ့ ဥပမာများ — SELECT, JOIN, GROUP BY, CASE ပေါင်းစပ်။"
autoNumbering: true
---

ဤစာမျက်နှာတွင် MySQL လက်တွေ့ ဥပမာ များစွာ — တစ်ခုချင်းစီကို လိုက်လုပ်၍ လေ့လာပါ။

## ဥပမာ 1 — Tables 2 ခု ဖန်တီး

```sql
CREATE TABLE customers (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL,
  city VARCHAR(50)
);

CREATE TABLE orders (
  id INT PRIMARY KEY AUTO_INCREMENT,
  customer_id INT,
  amount DECIMAL(10,2),
  FOREIGN KEY (customer_id) REFERENCES customers(id)
);
```

## ဥပမာ 2 — Data ထည့်

```sql
INSERT INTO customers (name, city) VALUES
('Aung', 'Yangon'),
('Su', 'Mandalay'),
('Mya', 'Yangon');

INSERT INTO orders (customer_id, amount) VALUES
(1, 150000), (1, 80000), (2, 95000);
```

## ဥပမာ 3 — Simple SELECT

```sql
SELECT name, city FROM customers ORDER BY name;
```

## ဥပမာ 4 — JOIN + GROUP BY

```sql
SELECT c.name, COUNT(o.id) AS order_count, SUM(o.amount) AS total
FROM customers c
LEFT JOIN orders o ON o.customer_id = c.id
GROUP BY c.id, c.name
HAVING COUNT(o.id) >= 1;
```

## ဥပမာ 5 — Status label (CASE)

```sql
SELECT name,
  CASE
    WHEN SUM(o.amount) > 200000 THEN 'VIP'
    ELSE 'Standard'
  END AS level
FROM customers c
LEFT JOIN orders o ON o.customer_id = c.id
GROUP BY c.id, c.name;
```

## အဓိက အချက်များ
- Tables ဆောက် → data → query
- JOIN, GROUP BY, HAVING, CASE ပေါင်း
- ကိုယ်တိုင် လိုက်လုပ်ကြည့်