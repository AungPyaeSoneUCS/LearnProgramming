---
title: "Decision Trees & Random Forests — Decision Trees & Random Forests"
description: "Decision tree — random forest — ဥပမာ။"
autoNumbering: true
---

Decision tree & Random Forest — အခြေ — လူ လို ဆင်ခြင်။

## Decision tree

- Questions စုံး
- Split — အချက်
- Leaf — answer

```js
const go = (weather, distance) => {
  if (weather === "rain") return false;
  if (distance > 10) return false;
  return true;
};
```

## အလုပ်

1. Best split ရှာ
2. Branch ခွဲ
3. Leaf ထိ — repeat

## Random forest

- Trees များ
- Bootstrap sample
- Vote — အကောင်း

## အကြောင်း

- ပြ ပေး — interpretable
- Forest — stable
- Overfit — နည်း

## အဓိက အချက်များ

- Tree — if/else
- Forest — vote
- Robust
