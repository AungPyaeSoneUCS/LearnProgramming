---
title: "ML Practice Problems — ML Practice"
description: "ML practice — လက် တွေ့ — ဖြေ — ဥပမာ။"
autoNumbering: true
---

Practice — လက် တွေ့ — ဖြေ ဆင်း။

## 1. Predict price

```js
const price = (area) => 200 * area + 10000;
console.log(price(60));
```

## 2. Normalize

```js
const norm = (x) => x / 100;
```

## 3. Cluster

> Customers — group 2 — လုပ်။

## 4. Confusion

> TP=40, FP=10, FN=5 — precision? recall? F1?

```js
const precision = 40 / (40 + 10);
const recall = 40 / (40 + 5);
const f1 = (2 * precision * recall) / (precision + recall);
```

## အဓိက အချက်များ

- ၁ — predict
- ၂ — normalize
- ၃ — metrics
