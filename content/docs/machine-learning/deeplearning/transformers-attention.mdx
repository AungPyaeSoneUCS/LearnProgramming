---
title: "Transformers & Attention — Transformers & Attention"
description: "Transformer — attention — ဥပမာ။"
autoNumbering: true
---

Transformer — attention — NLP အခြေ — LLM အခြေ။

## Attention

- Focus — အရေး စာလုံး
- Context — ပတ်ဝန်း
- Score — match

```js
const weights = words.map((w) => score(w, query));
const norm = weights.map((w) => w / sum(weights));
```

## Self-attention

- အလိုက် လေး — အဆက်
- အလျား — မခွဲ

## Transformer parts

- Encoder — ဖတ်
- Decoder — ထုတ်
- Multi-head — အကြိမ်

## Why

- Parallel — မြန်
- Long context — ရ
- LLM — GPT, BERT

## အဓိက အချက်များ

- Attention — focus
- Encoder/decoder
- LLM base
