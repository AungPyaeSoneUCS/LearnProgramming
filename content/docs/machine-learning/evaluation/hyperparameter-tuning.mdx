---
title: "Hyperparameter Tuning — Hyperparameter Tuning"
description: "Hyperparameter — tune — ဥပမာ။"
autoNumbering: true
---

Hyperparameter — သတ် — tune — အကောင်း။

## ဥပမာ

- Learning rate
- Hidden layers
- k (K-Means)
- Trees count

## နည်း

- Grid search
- Random search
- Manual

```js
const lrs = [0.01, 0.05, 0.1];
const results = lrs.map((lr) => trainAndEval(lr));
```

## စောင့်

- အတွင်း — train
- အပြင် — validation
- ရှောင် — test

## Best

- သေး စမ်း
- အဆင့် ချ
- Randomized

## အဓိက အချက်များ

- Config — အပြင်
- Tune — validation
- Grid/random
