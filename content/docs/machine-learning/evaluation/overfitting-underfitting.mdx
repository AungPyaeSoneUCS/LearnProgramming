---
title: "Overfitting & Underfitting — Overfitting & Underfitting"
description: "Variance — bias — ဥပမာ။"
autoNumbering: true
---

Overfit — memorize — underfit — too simple — balance။

## Overfitting

- လေ စစ် — memorize
- Train ကောင်း — test ဆိုး
- High variance

## Underfitting

- အခြေ — လေ
- Both ဆိုး
- High bias

```js
const deg1 = (x) => 2 * x + 1; // underfit?
const deg9 = polyfit9(x); // overfit!
```

## ဖြေ

- More data
- Regularization
- Simpler model
- Early stop

## Detect

- Plot — train vs test error
- Gap — overfit

## အဓိက အချက်များ

- Overfit — variance
- Underfit — bias
- Balance — လို
