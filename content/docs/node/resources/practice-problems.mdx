---
title: "Node.js Practice Problems — Practice Problems"
description: "Node.js Practice Problems — ‌ပိုခက် ‌ပြဿနာများ — ‌ဥပမာ။"
autoNumbering: true
---

Node.js ‌စိန်ခေါ်မှု — practice problems — ‌အဆင့်မြင့် ‌စမ်းသပ်။

## 1. URL shortener

- POST `/api/shorten` — long URL → short code
- GET `/:code` — redirect
- Storage — in-memory ‌ဖြစ်

## 2. File watcher

Directory ‌တွင် changes ‌မှတ် — `fs.watch` — ‌လော့ logs။

```js
fs.watch("data", (event, filename) => console.log(event, filename));
```

## 3. Rate-limited API

Per-IP tokens — ‌ဖြင့် ‌ကန့်သတ် 100 req/sliding window။

## 4. Todo REST + MySQL

## 4. Todo REST + MySQL

## 5. Chat with websockets

## 5. Chat with websockets

## 6. Prime search worker

Worker thread ‌ဖြင့် ‌ကြီး primes ‌ရှာ — main ‌မပိတ်။

## 7. Node store — middleware practice

Auth + logging + error middleware — Express ‌ပေါင်း build။

## ‌အဓိက အချက်များ
- Real mini projects
- Files + stores + middleware
- Workers — parallel ‌စမ်း