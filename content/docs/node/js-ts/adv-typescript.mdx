---
title: "Node.js Advanced TypeScript — Advanced TypeScript"
description: "Advanced TS in Node — generics, utility types, zod, decorators — ဥပမာများ။"
autoNumbering: true
---

Server code ‌တွင် ‌အသုံးများသော TypeScript ‌အဆင့်မြင့် ‌နည်းပညာများ။

## Generics — ‌ပြန်သုံးနိုင် functions

```ts
function first<T>(arr: T[]): T | undefined {
  return arr[0];
}

const n = first([1, 2, 3]);     // number | undefined
const s = first(["a", "b"]);    // string | undefined
```

## Utility Types

```ts
type User = { id: number; name: string; email?: string };

type UserInput = Omit<User, "id">;         // name, email?
type Safe = Partial<User>;                 // ‌အ‌လုံး optional
type Card = Readonly<User>;                // readonly
type Id = Extract<User["id"], number>;     // number
```

## Discriminated Unions

```ts
type Result =
  | { ok: true; data: string }
  | { ok: false; error: string };

function handle(r: Result) {
  if (r.ok) console.log(r.data);   // narrowed
  else console.error(r.error);
}
```

## Runtime validation — zod

```bash
npm install zod
```

```ts
import { z } from "zod";

const UserSchema = z.object({
  id: z.number(),
  name: z.string().min(1),
});

type User = z.infer<typeof UserSchema>;
const parsed = UserSchema.parse(req.body);  // throws if invalid
```

## ‌အဓိက အချက်များ
- Generics — reuse
- Utility types — Omit/Partial
- zod — runtime validation