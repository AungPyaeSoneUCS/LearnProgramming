---
title: "Module 17: File I/O & Networking — Module 17: File I/O & Networking"
description: "java.nio — serialization — HttpClient — modern I/O ။"
autoNumbering: true
---


Module 17 တွင် file I/O နှင့် networking ကို လေ့လာပါမည်။ Java 7 ၏ modern java.nio package နှင့် Java 11 ၏ HttpClient ကို အသုံးပြု၍ files များနှင့် network requests များကို ရှင်းလင်းစွာ ရေးသားနိုင်ပါသည်။

## File I/O — java.nio.Files

`Files` class သည် writeString, readString, delete စသည့် static methods များဖြင့် file operations များကို ရိုးရှင်းစွာ ပြုလုပ်ပေးပါသည်။ `Path.of` ဖြင့် file path object ကို ဖန်တီးပါသည်။

## Serialization

`ObjectOutputStream` နှင့် `ObjectInputStream` တို့ဖြင့် object တစ်ခုလုံးကို byte stream အဖြစ် ပြောင်း၍ file တွင် သိမ်းပြီး ပြန်လည် ဖတ်နိုင်ပါသည်။ Serialize လုပ်ရန် class သည် `java.io.Serializable` interface ကို implement ပြုလုပ်ရပါသည်။

## HTTP Requests — HttpClient

Java 11 မှစ၍ `java.net.http.HttpClient` ဖြင့် HTTP requests များကို ရေးနိုင်ပြီး GET/POST requests များကို response status နှင့် body နှင့်တကွ ရယူပါသည်။

## ဥပမာ — File Write/Read

```java
import java.nio.file.Files;
import java.nio.file.Path;
import java.util.List;

public class Main {
    public static void main(String[] args) throws Exception {
        Path file = Path.of("notes.txt");
        Files.writeString(file, "Line 1: Hello
Line 2: Java NIO");

        String content = Files.readString(file);
        System.out.println("Content:\n" + content);

        List<String> lines = Files.readAllLines(file);
        System.out.println("Lines: " + lines);

        Files.delete(file);
        System.out.println("File exists after delete? " + Files.exists(file));
    }
}
```

အထက်ပါ code တွင် Files.writeString ဖြင့် file ရေးပြီး readString ဖြင့် ဖတ်ပါသည်။ Run လုပ်သောအခါ အောက်ပါ output ကို ရမည် ဖြစ်ပါသည်။

```
Content:
Line 1: Hello
Line 2: Java NIO
Lines: [Line 1: Hello, Line 2: Java NIO]
File exists after delete? false
```

## ဥပမာ — Object Serialization

```java
import java.io.FileInputStream;
import java.io.FileOutputStream;
import java.io.ObjectInputStream;
import java.io.ObjectOutputStream;
import java.io.Serializable;
import java.nio.file.Files;
import java.nio.file.Path;

public class Main {
    public static void main(String[] args) throws Exception {
        Person person = new Person("Aung", 25);

        try (ObjectOutputStream out = new ObjectOutputStream(new FileOutputStream("person.ser"))) {
            out.writeObject(person);
        }

        try (ObjectInputStream in = new ObjectInputStream(new FileInputStream("person.ser"))) {
            Person loaded = (Person) in.readObject();
            System.out.println("Loaded: " + loaded.name + ", age " + loaded.age);
        }

        Files.deleteIfExists(Path.of("person.ser"));
    }
}

class Person implements Serializable {
    String name;
    int age;

    Person(String name, int age) {
        this.name = name;
        this.age = age;
    }
}
```

အထက်ပါ code တွင် try-with-resources ဖြင့် streams များကို အလိုအလျောက် close လုပ်ပြီး object တစ်ခုလုံးကို file ထဲတွင် သိမ်းဆည်းပြီး ပြန်လည် load ပါသည်။ Run လုပ်သောအခါ အောက်ပါကဲ့သို့သော output ကို ရမည် ဖြစ်ပါသည်။

```
Loaded: Aung, age 25
```

## ဥပမာ — HTTP GET Request

```java
import java.net.URI;
import java.net.http.HttpClient;
import java.net.http.HttpRequest;
import java.net.http.HttpResponse;

public class Main {
    public static void main(String[] args) throws Exception {
        HttpClient client = HttpClient.newHttpClient();

        HttpRequest request = HttpRequest.newBuilder()
                .uri(URI.create("https://api.github.com"))
                .GET()
                .build();

        HttpResponse<String> response =
                client.send(request, HttpResponse.BodyHandlers.ofString());

        System.out.println("Status = " + response.statusCode());
        String body = response.body();
        System.out.println("Body preview = " + body.substring(0, Math.min(60, body.length())));
    }
}
```

ဤ code သည် internet လိုအပ်၍ run လုပ်ရန်အတွက် network နှင့် ချိတ်ဆက်ထားရန် လိုပါသည်။ အောင်မြင်ပါက status 200 နှင့် JSON body ၏ အစိတ်အပိုင်းကို မြင်ရမည် ဖြစ်ပါသည်။

## အဓိက အချက်များ

- java.nio.Files ဖြင့် file ရေး/ဖတ် ရိုးရှင်းသည်။
- Serializable interface ဖြင့် object ကို serialize လုပ်နိုင်သည်။
- HttpClient ဖြင့် HTTP requests ကို ရေးနိုင်သည်။
- Try-with-resources ဖြင့် streams များကို ပိတ်ပါသည်။

